#include "monty.h"
/**
 * nop_stack - does nothing
 * @line_number: integer
 * @stack: head node of stack
 * Return: a new node
 */
void nop_stack(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
